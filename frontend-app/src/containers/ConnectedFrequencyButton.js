import FrequencyCountButton from '../components/FrequencyCountButton'
import { connect } from 'react-redux'

export const ConnectedFrequencyButton = connect()(FrequencyCountButton);

export default ConnectedFrequencyButton